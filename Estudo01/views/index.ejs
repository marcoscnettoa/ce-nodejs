<!DOCTYPE html>
<html lang="pt">
    <head>
        <meta charset="UTL-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta http-equiv="X-UA-Compatible" content="ie=edge">
        <link rel="stylesheet" href="/css/style.css">
        <title>Projeto 01</title>
    </head>
    <body>
        <h3>Olá! Bem vindo <%=nome%>!</h3>
        <p>Você está aprendendo a linguagem <strong><%=lang%></strong></p>
        <% if(exibirVersao) { %>
            <p><i>Versão: <%=versao%></i></p>
        <% } else { %>
            <p><i>Versão não disponível!</i></p>
        <% } %>
        <br/>
        <h3>Lista de Produtos</h3>
        <ul>
            <% produtos.forEach(function(produto) { %>
                <li><strong>Nome:</strong> <%= produto.nome %> -| <strong>Valor:</strong> R$ <%= produto.preco %></li>
            <% }) %>
        </ul>
    </body>
</html>